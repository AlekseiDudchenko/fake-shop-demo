# СК «Идеал» — лендинг строительных работ (Томск и область)

Статическая одностраничная посадочная страница строительной компании: услуги,
этапы работ, цены, география выезда, отзывы и форма заявки.
Один файл `index.html`, без сборки и бэкенда.

## Как открыть

Дважды кликнуть по `index.html` — откроется в браузере.

## Как адаптировать под себя

Всё правится прямо в `index.html`:

1. **Название компании** — блок `<div class="logo">` в шапке и текст в `<footer>`.
2. **Телефон и адрес** — `+7 (913) 852-16-10`, `info@example.ru` и адрес офиса
   в шапке и в секции `#contact` (не забыть про `href="tel:..."`).
3. **Услуги и цены «от»** — карточки в секции `#services`.
4. **Прайс** — таблица `table.prices` в секции `#prices`.
5. **География** — список `.geo-list` в секции `#geo`.
6. **Отзывы** — блоки `.review` в секции `#reviews`.
7. **Цвета** — переменные `--accent`, `--dark` в `:root` в начале `<style>`.

## Форма заявки

Форма демонстрационная: данные никуда не отправляются, показывается только
модальное окно с подтверждением (функция `submitLead` внизу файла).
Для реальной отправки замените её на отправку в CRM, на почту или в Telegram-бота.

## Публикация (по желанию)

Settings → Pages → Source: `main` / root. Страница будет доступна по адресу
`https://<username>.github.io/fake-shop-demo/`.

## Варианты вёрстки

Кроме основной страницы в репозитории лежат пять альтернативных вёрсток
с теми же данными (название, телефон, услуги, цены, география, отзывы):

| Адрес | Стиль |
|---|---|
| `/v1/` | тёмная «промышленная», плитки-бенто, жёлтый акцент |
| `/v2/` | светлый минимализм, серифная типографика, много воздуха |
| `/v3/` | фиксированная боковая навигация, синий корпоративный |
| `/v4/` | жёстко-контрастная жёлто-чёрная, крупные заголовки |
| `/v5/` | мобильная, «как приложение», липкая панель звонка снизу |

Каждый вариант — один самодостаточный `index.html` в своей папке.
Вверху каждой страницы есть переключатель между вариантами.

## ВентСай-Томск

Второе направление, отдельный набор страниц в папке `ventsai/`:
ООО «ВентСай-Томск» — изделия для фасада, кровли и вентиляции
(воздуховоды и фасонные детали, водосточные системы, фасадные кассеты
и доборные элементы, плазменный раскрой металла, изделия по чертежам,
товары для сада и огорода), а также монтаж вентиляции.

| Адрес | Стиль |
|---|---|
| `/ventsai/` | страница-указатель со ссылками на три варианта |
| `/ventsai/v1/` | тёмный инженерный, бирюзовый акцент, липкая шапка |
| `/ventsai/v2/` | светлый корпоративный, синий с оранжевым, форма заявки в первом экране |
| `/ventsai/v3/` | сплит-экран, вкладки по направлениям |

### Данные

Содержание собрано по сайту ventsay70.ru: направления работы, история
компании (более 11 лет в Томске), руководители, телефон 8 (3822) 577-002.
Тексты написаны заново, не скопированы.

Требуют проверки: почта `ventsay@mail.ru` и адрес
«г. Томск, ул. Профсоюзная, 28/1, стр. 3» — взяты из справочников,
а не с самого сайта. Цен на страницах нет: вместо прайса — таблица
номенклатуры с материалами и размерами.

Фотографии лежат в `ventsai/img/` (см. `ventsai/img/README.md`).

### Превью ссылки (Open Graph) — только v3

`ventsai/v3/` отдаёт корректную карточку при вставке ссылки в мессенджеры:
`og:title`, `og:description`, `og:image` (1200×630, файл `ventsai/v3/og.jpg`),
`twitter:card`, `canonical` и `theme-color`.

Картинка `og.jpg` — фирменный баннер: логотип, перечень изделий и три
преимущества поверх фото цеха. Синие акценты исходника перекрашены в `--acc`
сайта (`#c8102e`), фото обесцвечено до нейтральной стали, тёмная панель
доведена до `--ink` (`#1a1a1a`) — карточка в мессенджере и сама страница
теперь в одной палитре. Пересобрать можно в любой момент, важно сохранить
размер 1200×630.

**При переезде на свой домен** обязательно поменяйте абсолютные адреса
в `og:url`, `og:image`, `og:image:secure_url`, `twitter:image` и `canonical`:
сейчас там ссылки на GitHub Pages. Относительные пути в этих тегах не работают —
мессенджеры их не понимают.

### Согласие на обработку персональных данных — v3

В форме заявки на `ventsai/v3/` стоит обязательный чекбокс согласия
со ссылкой на страницу `ventsai/v3/privacy/` — политику обработки
персональных данных (152-ФЗ). Ссылка на политику продублирована в подвале.
Страница закрыта от индексации (`noindex, follow`).

**Перед публикацией нужно:**

1. Добавить в блок «Оператор» на странице политики ИНН и ОГРН — сейчас их там нет.
2. Подтвердить адрес и почту: `ventsay@mail.ru` и «Профсоюзная, 28/1, стр. 3»
   взяты из справочников, а не с сайта компании.
3. Проверить срок хранения данных (сейчас указано «не более трёх лет
   с момента последнего обращения») — он должен совпадать с реальной практикой.
4. Дать текст юристу: это рабочий шаблон, а не проверенный юридический документ.

Чекбокс сейчас только блокирует отправку формы. Когда появится реальная
отправка заявок, факт согласия нужно сохранять вместе с заявкой
(дата, время, версия политики).

### Подписи к работам и блок «Сроки и отгрузка» — v3

В галерее «Наши работы» у каждой фотографии теперь заголовок и строка о том,
что на ней происходит, — вместо повтора одного и того же названия.
Подписи описывают только то, что видно на снимке: заказчики, объекты и даты
не выдуманы и не указаны.

Ниже блока «Как заказать» добавлен раздел «Сроки и отгрузка» (`#terms`):
сроки изготовления, самовывоз, доставка, оплата и документы.

**Перед публикацией нужно сверить с производством** (в HTML стоит пометка
`ПОДТВЕРДИТЬ`):

1. Сроки «1–3 рабочих дня» на типовые изделия — это заготовка, а не факт.
2. Зону доставки: сейчас указаны Томск, Северск и районы области
   транспортной компанией.
3. Формулировку про оплату — если компания работает с НДС, это стоит
   написать прямо: для организаций это важный фильтр при выборе поставщика.

### Объединённые разделы услуг — v3

«Направления» (вкладки) и «Что мы делаем» (карточки) перечисляли одно и то же
разными словами. Теперь это один раздел `#services`: вводный текст о компании
и шесть карточек, в каждой — описание и список того, что входит.

Вкладки убраны вместе с их разметкой, стилями и обработчиком `tab()`.
Побочный эффект — весь текст услуг теперь виден сразу, без клика:
это лучше и для поисковой выдачи, и для телефона. Отдельный пункт про
«вкладки → аккордеон» из списка улучшений после этого не нужен.

Фоны секций пересобраны в прежнее чередование: `#steps` стал светлым,
`#terms` — серым.

### Переключатель вариантов снят с v3

Тёмной полосы «варианты: v1 v2 v3» на `ventsai/v3/` больше нет — вместе
с её разметкой и стилями. Посетитель боевого сайта не должен видеть
черновики: это выглядит как незаконченная работа и уводит с формы заявки.

На `ventsai/v1/` и `ventsai/v2/` переключатель остался — это черновики
для сравнения. Ссылка с них на v3 работает, обратной ссылки с v3 нет:
так и задумано. Список вариантов по-прежнему на `ventsai/`.

### Иконка сайта и разметка организации — v3

**Favicon.** `ventsai/v3/favicon.svg` (масштабируемая), `favicon.ico`
(16/32/48 px для старых браузеров) и `apple-touch-icon.png` (180 px, для
иконки на домашнем экране iOS). Рисунок — три полосы решётки на тёмном
фоне, нижняя красная: на 16 px читается как вентиляционная решётка,
а не как размытые буквы. Иконки подключены и на странице политики.

**Schema.org LocalBusiness.** В `<head>` страницы v3 добавлен блок
JSON-LD: название и юрлицо, телефон, почта, адрес, зона работы
(Томск, Северск, область) и каталог из семи направлений. Это то, из чего
поисковик собирает карточку организации в выдаче.

**Перед запуском** (в HTML стоит пометка `ПОДТВЕРДИТЬ`):

1. Сверить адрес, почту и добавить ИНН/ОГРН.
2. Добавить `openingHours` — часы работы намеренно не указаны, потому что
   они неизвестны; выдумывать их в разметке нельзя, по ней людям звонят.
3. При переезде на свой домен заменить абсолютные адреса в `@id`, `url`
   и `image` — там сейчас адрес GitHub Pages.

Проверить разметку после публикации: search.google.com/test/rich-results
и Яндекс.Вебмастер → «Валидатор микроразметки».

### Липкая кнопка звонка — v3

На экранах уже 800 px внизу закреплена панель «Позвонить / Оставить заявку»
(`.callbar` в `ventsai/v3/index.html`). Учтено:

- `env(safe-area-inset-bottom)` — панель не уезжает под жест-бар iPhone;
- `body{padding-bottom:78px}` в том же медиазапросе — панель не перекрывает подвал;
- панель прячется, пока форма заявки видна на экране (IntersectionObserver),
  чтобы не закрывать поля;
- на десктопе панель скрыта: там телефон и кнопка есть в шапке.

Мессенджеров в панели нет намеренно: известен только городской номер
8 (3822) 577-002, а WhatsApp и Telegram нужен мобильный. Дадите мобильный —
добавлю кнопки.

### Калькулятор воздуховодов — v3

Секция `#calc` на `ventsai/v3/`: посетитель задаёт сечение, длину, материал
и количество, страница показывает **площадь развёртки, вес металла и
ориентировочную цену**. Позиций можно набрать сколько нужно; кнопка
«Отправить расчёт заявкой» переносит весь список в поле комментария формы
и подставляет услугу — заказчику не приходится переписывать размеры руками.

**Что считается честно (правки не требует):**

- площадь развёртки: круг — `π · D · L`, прямоугольник — `2 · (A + B) · L`,
  плюс коэффициент 1,08 на фальцы и нахлёсты;
- вес: площадь × толщина × 7,85 кг/м²;
- автоподбор толщины по СНиП 2.04.05 / СП 60.13330 (круглые: до 200 мм — 0,5;
  до 450 — 0,6; до 800 — 0,7; далее 1,0; прямоугольные: до 250 — 0,5;
  до 1000 — 0,7; далее 0,9);
- фасонная деталь считается по приведённой длине развёртки ≈ 1,5 габарита.

**ПОДТВЕРДИТЬ ПЕРЕД ЗАПУСКОМ** — блок «ТАРИФЫ» в скрипте калькулятора
(`ventsai/v3/index.html`, комментарий «Калькулятор воздуховодов»).
Сейчас там ориентировочные цифры, их нужно заменить на расценки цеха:

| Переменная | Что это |
|---|---|
| `PRICE_M2` | цена за 1 м² развёртки оцинковки по толщинам, ₽ |
| `MATERIAL` | множители к цене оцинковки для нержавейки и чёрного металла |
| `FITTING_K` | во сколько раз фасонная деталь дороже прямого участка той же площади |
| `FOLD_K` | запас металла на фальцы и нахлёсты |
| `MIN_ITEM` | минимальная стоимость одного изделия, ₽ |

Под итогом стоит оговорка, что расчёт ориентировочный и не является офертой.

Под калькулятором — чертёж перехода прямоугольный–круглый
(`ventsai/img/drawing-transition.jpg`, кликается на полный размер).
Обозначения A, B и Ø D на нём те же, что в полях калькулятора, — заодно
показывает, что изделия делаются по чертежу.

### Отправка заявок — v3

Форма на `ventsai/v3/` шлёт данные в `send.php` (обычный PHP-хостинг, функция `mail()`).

**Настройка** — блок «НАСТРОЙКИ» в начале `ventsai/v3/send.php`:

| Переменная | Что поставить |
|---|---|
| `$TO` | адрес(а), куда приходят заявки |
| `$FROM` | адрес **на вашем домене**, иначе письма уйдут в спам (SPF) |
| `$SUBJECT` | тема письма |
| `$POLICY_VERSION` | дата версии политики — пишется в журнал как доказательство согласия |
| `$LOG_FILE` | путь к журналу; лучше **выше корня сайта** |
| `$RATE_LIMIT` | сколько заявок с одного IP в час (по умолчанию 5) |

**Что уже сделано в обработчике:**

- проверка полей: имя от 2 символов, телефон минимум 10 цифр, обязательное согласие;
- защита от ботов: honeypot-поле `company` и отсечка отправок быстрее 3 секунд;
- ограничение частоты по IP;
- защита от подстановки почтовых заголовков (переводы строк вырезаются);
- журнал заявок с отметкой о согласии и версией политики;
- ответы в JSON, страница показывает текст ошибки и не теряет заполненные поля.

`.htaccess` рядом закрывает прямой доступ к `*.log` — в журнале персональные данные.
На nginx это правило нужно перенести в конфиг сервера. `leads.log` добавлен в `.gitignore`.

**Демо-режим.** На GitHub Pages PHP не выполняется, поэтому там форма показывает
подтверждение, ничего не отправляя (проверка `location.hostname` в скрипте страницы).
На боевом хостинге режим включится сам и заявки пойдут на почту.
